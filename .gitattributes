# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts must stay LF even on Windows checkouts
# (CRLF breaks bash: "$'\r': command not found")
*.sh text eol=lf

# Windows batch files always use CRLF
*.bat text eol=crlf

